{
  "name": "freat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch server.js & server=$!; trap 'kill $server 2>/dev/null' EXIT; vite",
    "build": "vite build",
    "preview": "bun run build && bun server.js",
    "start": "bun server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
