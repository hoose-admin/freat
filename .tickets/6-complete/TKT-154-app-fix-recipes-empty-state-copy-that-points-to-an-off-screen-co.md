---
id: TKT-154
title: "Fix recipes empty-state copy that points to an off-screen control"
status: "Complete"
priority: "Medium"
assignee: "Claude-Agent"
created: 2026-06-22
completed: 2026-06-22
domain: "app"
tags:
  - ux
  - ai-proposed
depends_on: []
blocks: []
related: []
files_touched:
  - "src/components/RecipeList.tsx"
complexity: 1
next_step_hint: Verify AC with a fresh subagent — empty-state copy at RecipeList.tsx:35-38 now names the on-screen Edit ingredients button.
chaos_branch: chaos/TKT-154
merged: 2026-06-22
merge_commit: 0d90182c5b09
---

## Objective
Fix the recipes empty-state guidance, which currently tells the user to do something they cannot do from the screen they are on.

## Context
When `recipes.length === 0`, `RecipeList` renders "No recipes yet. Try adding a few more ingredients." (`src/components/RecipeList.tsx:8-9`). But this only appears **after** `handleGetRecipes` resolves and the app is on the recipes phase (`src/App.tsx:33-45`), where there is **no ingredient input** — the only controls are "Edit ingredients" / "New photo" (`src/App.tsx:95-102`). The advice "add a few more ingredients" describes a control that lives on the *previous* screen, so it's a dead-end instruction that forces the user to recall the prior layout. It also implies the user hasn't acted, when in fact the model returned zero recipes.

## Acceptance Criteria
- `src/components/RecipeList.tsx` empty-state string (the `recipes.length === 0` branch) names the **Edit ingredients** control, which is the button actually rendered on the recipes screen (`src/App.tsx:96-98`) — not the off-screen ingredient input.
- The copy phrases the result as "came back empty" (e.g. "No recipes matched"), not as user inaction ("No recipes yet").
- No other behaviour, props, or markup of `RecipeList` changes; the non-empty render path is untouched.
- `bun run typecheck` passes.

### Pass-2 review

**Run:** 2026-06-22
**Reader:** cold (no pass-1 context carried)

- **AC tightening:** 3 bullets rewritten — empty-state branch now anchored to the on-screen control (`src/App.tsx:96-98`), the "empty result vs inaction" phrasing made checkable, and a no-collateral-change guard added.
- **Blockers:** ok — `depends_on` empty.
- **Context drift:** ok — `RecipeList.tsx:8-9`, `App.tsx:33-45` (handleGetRecipes), `App.tsx:95-102` (actions), `App.tsx:96-98` (Edit ingredients button) all verified present in the worktree.
- **Complexity:** ok — 1 (single-string copy change in one component).

**Verdict:** build-ready

### Autonomous Decision

**Made:** 2026-06-22 (chaos mode — no human input)
**Question:** The recipes empty-state was already rebuilt by TKT-103 (commit `49b14c67`) — it renders an on-screen **Edit ingredients** button and "No recipes matched…" copy. Does TKT-154 still have work, and if so what is the minimal change?

**Options considered:**
- **A — Close as resolved-by-TKT-103 (no code change)** — the empty-state already shows an on-screen Edit ingredients button, so the UX dead-end the ticket describes is already gone; arguably nothing to build.
- **B — Minimal copy edit so the string itself names the control** — AC1 specifically requires the empty-state *string* to name the "Edit ingredients" control; the live copy ("Add a couple more and try again") still doesn't, so one surgical line closes the letter-of-AC gap and ties the prose to the button below it.

**Chosen:** B — AC1 is explicit that the empty-state string must name the on-screen control (`src/components/RecipeList.tsx:35-38`); TKT-103's button satisfied the *spirit* but the prose still didn't *name* the control. Edited only the empty-state `<p>` to "…Tap **Edit ingredients** to add a couple more, then try again", reusing the in-component `<strong>` convention (`RecipeList.tsx:69,75`). The non-empty render path, props, and the existing button are untouched. Reversible single-line change; chose it over a no-op because the ticket's first AC is a literal copy assertion the live string did not yet meet.
**Reversibility:** easy — revert one line in `RecipeList.tsx`.

**Note on prior blocks:** this ticket previously carried `### Test Results` / `### Smoke Check` / `### Validation Review` / `### Implementation Summary` describing a `<strong>`-text change that was **never committed** (the worktree was clean against `main` when picked up — a phantom prior run). Those stale blocks were regenerated by this run to reflect the actual diff.

### UX Finding
**Heuristic:** Match between system & real world (Nielsen #2); recognition over recall (#6)
**Where:** `src/components/RecipeList.tsx:8-9`
**Now:** The recipes empty-state tells users to "add ingredients," but that input isn't on the recipes screen — it's behind the "Edit ingredients" button on the previous phase.
**Proposed:** Rewrite the empty-state to point at the on-screen "Edit ingredients" control and acknowledge the empty result.
**Why it helps:** Guidance matches what the user can actually see and do, instead of referencing an off-screen control.
**Impact:** med · **Effort:** low

### Implementation Summary

- Rewrote the `recipes.length === 0` empty-state `<p>` in `src/components/RecipeList.tsx` (lines 35-38) from `"No recipes matched those ingredients yet. Add a couple more and try again."` to `"No recipes matched those ingredients yet. Tap <strong>Edit ingredients</strong> to add a couple more, then try again."` — the copy now names the on-screen **Edit ingredients** button, which is rendered just below it (`RecipeList.tsx:38-42`, wired from `App.tsx:119`).
- Used `<strong>` for the control name, matching the existing `<strong>` usage already in this component (`RecipeList.tsx:69,75`), so no new markup/styling convention is introduced.

**Deviations from plan:**
- The empty-state button and the "No recipes matched" framing the AC envisioned already existed — added by TKT-103 (commit `49b14c67`) before this ticket was picked up. The only remaining gap was the copy *naming* the control, so this was a single-line change rather than a full empty-state rewrite. See the `### Autonomous Decision` block.

**Implementation notes:**
- `bun run typecheck` passes (exit 0).
- Diff confined to `src/components/RecipeList.tsx`, empty-state branch only; non-empty path, props, and the existing button untouched.

### Test Results

**Verifier:** fresh subagent (`general-purpose`)
**Run:** 2026-06-22
**Overall:** PASS

| AC | Pass | Evidence |
|---|---|---|
| Empty-state names the on-screen **Edit ingredients** control | ✓ | `RecipeList.tsx:36` empty-state `<p>` now reads "Tap **Edit ingredients** to add a couple more, then try again"; the control is on-screen — empty-state ghost button (`RecipeList.tsx:39-43`, wired from `App.tsx:119`) + persistent primary button (`App.tsx:124-130`) |
| Copy frames result as empty, not inaction | ✓ | `RecipeList.tsx:36` starts "No recipes matched those ingredients yet." — search came back empty, not "No recipes yet" |
| No other RecipeList behaviour/props/markup changed | ✓ | `git diff --stat`: `1 file changed, 2 insertions(+), 1 deletion(-)`; hunk touches only the empty-state `<p>` (35-38); props (6-10), non-empty render (50-108), empty-state button (39-43) unchanged |
| `bun run typecheck` passes | ✓ | `tsc --noEmit` produced no output, `EXIT_STATUS=0` |

**Commands run:**
- `git diff -- src/components/RecipeList.tsx`
- `git diff --stat`
- `bun run typecheck`

**Notes:** Diff is exactly the single `<p>` change. "Edit ingredients" is split across two source text lines but renders as one phrase. Only `RecipeList.tsx` is modified in the working tree.

### Smoke Check

**Headless Chromium:** SKIPPED (playwright not installed in `.weave` — `bun .weave/scripts/smoke.ts --ticket TKT-154` returned `{"status":"skipped","reason":"playwright not installed in .weave"}`)

Browser binaries are not provisioned in this worktree, so the deterministic smoke run no-ops (a skip is not a pass and does not fail the ticket). The change is a static empty-state string in `RecipeList`, on a render path that requires no Gemini call; `bun run typecheck` (exit 0) covers compile-time correctness.

### Validation Review

**Reviewer:** fresh subagent (`general-purpose`, distinct from test subagent)
**Run:** 2026-06-22
**Overall:** PASS

| Axis | Pass | Evidence |
|---|---|---|
| Objective fidelity | ✓ | `RecipeList.tsx:36-37` now names the on-screen **Edit ingredients** control (label matches the ghost button `RecipeList.tsx:41` and primary button `App.tsx:129`); prior copy pointed at an off-screen input with no such control on the recipes phase |
| Context constraints | ✓ | No Gemini/key/fetch code touched; single `<p>` string in a presentational component; new `<strong>` matches in-component convention (`RecipeList.tsx:70,76`); AI-laziness + PWA untouched; `tsc --noEmit` passes |
| Sprawl | ✓ | `git status --short` reports only `M src/components/RecipeList.tsx` — exactly `files_touched`; single hunk, no incidental edits |
| Follow-up surfacing | ✓ | One low-priority, pre-existing observation: two "Edit ingredients" affordances on the empty recipes screen (`RecipeList.tsx:39-43` + `App.tsx:124-130`) — both predate this diff (TKT-103 + App shell), not a regression |

**Suggested new tickets:** 1 (deferred to backlog — see below)

**Architecture coherence:** Fully coherent. Self-contained microcopy edit in a presentational component; honors ADR-001 (no client-side AI call, no Gemini key reference, no new data-fetching path — the `src/lib/api.ts` funnel and `/api/*` boundary are untouched), reuses the existing `onEditIngredients` wiring (`RecipeList.tsx:9,39-43` ↔ `App.tsx:119`) and the in-component `<strong>` convention. No parallel/conflicting pattern introduced; CLAUDE.md hard rules 1–4 unaffected.
